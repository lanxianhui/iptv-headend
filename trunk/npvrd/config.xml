<?xml version="1.0" encoding="utf-8"?>
<config>
	<database>
		<userName>iptv-test</userName>
		<password>iptv-test</password>
		<databaseLocator>jdbc:mysql://localhost:3306/cm_iptv?useUnicode=true&amp;characterEncoding=utf-8</databaseLocator>
		<databaseDriver>com.mysql.jdbc.Driver</databaseDriver>
	</database>
	<recordings>./recordings/</recordings> <!-- Where to write all recorded programs (should be writable by the npvrd -->
	<schedUpdaterInterval>10</schedUpdaterInterval> <!-- How much to wait to refresh the recording schedule (in seconds, global for all SUs) -->
</config>